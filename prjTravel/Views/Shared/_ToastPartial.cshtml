@if (TempData["Success"] != null)
{
    <div id="liveToast" class="toast align-items-center text-white bg-success border-0 top-0 end-0 position-absolute" role="alert" aria-live="assertive" aria-atomic="true" style="margin-top: 60px; margin-right: 4px;">
        <div class="d-flex">
            <div class="toast-body">
                @TempData["Success"]
            </div>
            <button type="button" class="btn-close btn-close-white me-2 m-auto" data-bs-dismiss="toast" aria-label="Close"></button>
        </div>
    </div>

    <script>
        var toastLiveExample = document.getElementById('liveToast');
        var toast = new bootstrap.Toast(toastLiveExample);
        toast.show();
    </script>
}
@if (TempData["Error"] != null)
{
    <div id="liveToast" class="toast align-items-center text-white bg-danger border-0 top-0 end-0 position-absolute" role="alert" aria-live="assertive" aria-atomic="true" style="margin-top: 60px; margin-right: 4px;">
        <div class="d-flex">
            <div class="toast-body">
                @TempData["Error"]
            </div>
            <button type="button" class="btn-close btn-close-white me-2 m-auto" data-bs-dismiss="toast" aria-label="Close"></button>
        </div>
    </div>
    <script>
        var toastLiveExample = document.getElementById('liveToast');
        var toast = new bootstrap.Toast(toastLiveExample);
        toast.show();
    </script>
}